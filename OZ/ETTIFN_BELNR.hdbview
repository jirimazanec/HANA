schema="OTEBW_OZ";

query="SELECT
ETTIFN.ANLAGE as ANLAGE,
ETTIFN.AB as AB,
ETTIFN.BIS as BIS, 
ETTIFN.BELNR as BELNR
FROM OTEBW_OZ.ETTIFN WHERE
	ETTIFN.MANDT = 400 AND
	BELNR <> ' '
GROUP BY ANLAGE, AB, BIS, BELNR";