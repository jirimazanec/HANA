schema="OTEBW_OZ";

query= "SELECT GENERATION_PLANT as GENERATION_PLANT, SUM(POWER) as POWER from \"OTEBW_OZ\".\"ZOZE_SOURCE\"
WHERE  STATUS = 'ACCEPTED' AND
	   MANDT = '400'
GROUP BY MANDT, GENERATION_PLANT
ORDER BY GENERATION_PLANT ASC"; 